<?php
    //  Tampilkan tanggal
    // echo date("l, d-M-Y");
    // echo time();
    // echo date("d M Y", time()+60*60*24*12000);

    // mktime
    // membuat sendiri detik
    // mktime(0,0,0,0,0,0)
    // jam, menit, detik, bulan, tanggal, tahun
    // echo date("l", mktime(0,0,0,11,25,2003));

    // strtotime
    // echo date("l",strtotime("25 aug 20003"));

    // STR
    // strlen() => untuk menghitung panjang sebuah string
    // strcmp() => membandingkan 2 string
    // explode() => pecah string jadi array
    //  htmlspecialchars() => jaga kita dari orang jahil

    // utility
    // var_dump() => ceetak isi dari variabel array
    // isset(_ =>apakaj sebuah variabel sudah dibaut atau belum
    // empty()=> cek variabel kosong / tidak
    // die() => untuk memberhentikan program
    //  sleepr() => berhenti sementara



?>